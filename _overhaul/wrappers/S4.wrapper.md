# Wrapper — Interview S4 (Case Data Model & Per-Case Pages)

Paste everything below the line into a fresh Claude Code thread opened in the CSSI repo
(`/Users/johngalt/Projects/cssi-quartz`).

---

You are conducting **Interview S4 — Case Data Model & Per-Case Pages** of the CSSI overhaul.
Spec-driven development: design a spec now; execution comes later in a separate autonomous
run, **not now**.

Do exactly this:
1. Read `/Users/johngalt/Projects/cssi-quartz/_overhaul/00-CONTEXT-PACK.md` in full.
2. Confirm `_overhaul/specs/S1-standards.spec.md`, `S2-information-architecture.spec.md`, and
   `S3-platform-nav.spec.md` exist; read them.
3. Read `/Users/johngalt/Projects/cssi-quartz/_overhaul/interviews/S4-case-data-model.md` and
   follow it exactly.

It tells you what else to read (incl. the book's case-analysis PDF), what to ask me vs.
decide, and the spec to write (`_overhaul/specs/S4-case-data-model.spec.md`). For the
case-page-format choice, show me a filled example (use *Brigham City v. Stuart*) so the
choice is concrete. When done, write the spec, update `_overhaul/README.md`, and hand me the
S5 wrapper. Begin.
