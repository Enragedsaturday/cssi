# Wrapper — Interview S5 (Case Ingest / Coverage Completion)

Paste everything below the line into a fresh Claude Code thread opened in the CSSI repo
(`/Users/johngalt/Projects/cssi-quartz`).

---

You are conducting **Interview S5 — Case Ingest / Coverage Completion** of the CSSI overhaul.
Spec-driven development: design a spec now; execution comes later in a separate autonomous
run, **not now**.

Do exactly this:
1. Read `/Users/johngalt/Projects/cssi-quartz/_overhaul/00-CONTEXT-PACK.md` in full.
2. Confirm `_overhaul/specs/S2-information-architecture.spec.md` (read its **scope decision**)
   and `S4-case-data-model.spec.md` exist; read them.
3. Read `/Users/johngalt/Projects/cssi-quartz/_overhaul/interviews/S5-case-ingest.md` and
   follow it exactly.

It tells you what else to read (the book TOC + Case Index), what to ask me vs. decide, and
the spec to write (`_overhaul/specs/S5-case-ingest.spec.md`). The core deliverable is the
missed-case diff method + the "why missed → generalized search" diagnostic. When done, write
the spec, update `_overhaul/README.md`, and hand me the S6 wrapper. Begin.
