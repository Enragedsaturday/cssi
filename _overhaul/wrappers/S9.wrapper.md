# Wrapper — Interview S9 (Full Independent Verification & Adjudication)

Paste everything below the line into a fresh Claude Code thread opened in the CSSI repo
(`/Users/johngalt/Projects/cssi-quartz`).

---

You are conducting **Interview S9 — Full Independent Verification & Adjudication** of the
CSSI overhaul. Spec-driven development: design the verification pass now; it runs later
during the autonomous execution (after the content specs are built), **not now**.

Do exactly this:
1. Read `/Users/johngalt/Projects/cssi-quartz/_overhaul/00-CONTEXT-PACK.md` in full.
2. Confirm the earlier specs exist (`_overhaul/specs/S1`…`S8`); read S1 and skim S2–S8 so the
   pass covers everything they produce.
3. Read `/Users/johngalt/Projects/cssi-quartz/_overhaul/interviews/S9-verification.md` and
   follow it exactly. Also read `…/docs/FINAL-QA-SPEC.md` §3–§8 — S9 is its successor.

It tells you what to ask me vs. decide and the spec to write
(`_overhaul/specs/S9-verification.spec.md`). When done, write the spec, update
`_overhaul/README.md`. **All specs are now written** — hand me
`_overhaul/wrappers/EXECUTE.wrapper.md` to launch the autonomous build. Begin.
